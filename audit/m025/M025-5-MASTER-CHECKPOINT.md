# M025-5 MASTER Checkpoint

**Last updated:** 2026-08-16 Asia/Jakarta  
**Repository:** `fitrajft-ux/FIEZEL-APPS`  
**Control issue:** #12  
**Squad relay:** #22  
**Neural investigation/evidence:** #30  
**Candidate PR:** #37 (DRAFT until promotion is coherent and all exact-head gates pass)

## 1. Start here

Do not restart this investigation from generic WebKit/ONNX speculation.

Production `main` is still the m025-4 neural diagnostic baseline. The active candidate branch is:

`master/m025-5-safari-phonemizer-compat`

Authoritative candidate state before runtime promotion:

`SOURCE_CANDIDATE_PROVEN / PROMOTION_BLOCKED`

The reason for the block is mechanical Git-object transport of the exact proven vendor bundle, **not** uncertainty about source, build, artifact availability, or deterministic output.

## 2. Physical evidence that led here

The Apple standalone neural stall was narrowed before tokenizer entry. m025-4 added source-derived, phase-only eSpeak lifecycle diagnostics without persisting prompt/phoneme/token content or raw external error messages.

The current diagnosis lane is the pinned phonemizer/eSpeak worker lifecycle, not model/ONNX inference as the first hypothesis unless a newer physical trace contradicts it.

Physical Apple evidence remains authoritative. CI/Pages success alone is never a physical neural-success claim.

## 3. Frozen runtime semantics

Do not change these merely to make a gate pass:

- Apple standalone neural generation timeout: existing 30 s contract;
- m026 single-flight/no-hidden-retry behavior;
- model and model revision;
- six voice assets / selected voice behavior;
- Puter/auth behavior;
- fallback/playback behavior;
- FIEZEL version/cache version unless independently justified;
- Pages deployment architecture merely as a workaround.

## 4. Exact upstream Safari compatibility evidence

The staged m025-5 source candidate uses the exact upstream phonemizer worker fix rather than a hand-authored app-layer ReadableStream shim.

Locked source relation:

- phonemizer repository: `xenova/phonemizer.js`
- base commit: `6835144b7ee9043129222549c1ed2f6a27216278`
- upstream PR: `#4`
- exact upstream head: `6cef703722c7e867029665df9c85ccaadc4ef19c`
- only upstream source path changed for this compatibility fix: `src/espeakng.worker.js`
- exact upstream worker blob Git SHA: `3c2cc8ca249d5b8011b67b7c4041567dae184e94`

The repository builder fetches/consumes this exact source head and then rebuilds the FIEZEL vendor from the source-locked Kokoro integration path.

## 5. Deterministic candidate bundle

Two independent source-derived builds produced byte-identical output.

Authoritative staged candidate:

- path after promotion: `vendor/kokoro-js/kokoro.web.js`
- SHA-256: `f4329122c16919d170cb88d071b7d22066a4567d4f76feec2d3273412be145ad`
- size: `2,136,684` bytes
- local Git blob SHA for those exact bytes: `7c8f6353f98fb1516286db20f914b2cdc5537e16`

The Git blob SHA above was queried against this repository and is **not currently present** in the repository object database.

## 6. Exact artifact recovery

The exact proven bytes are recoverable; do not rebuild solely because you think the artifact is missing.

Successful candidate build:

- workflow: `FIEZEL m025-5 Read-only Candidate Build`
- run: `31911875819`
- head: `e6771da3f96e412e5d55a494781bf08c9dacfbc6`
- artifact ID: `9253887164`
- artifact name: `m025-5-vendor-candidate-e6771da3f96e412e5d55a494781bf08c9dacfbc6`
- artifact retention in that run: 3 days

MASTER downloaded and extracted the artifact and independently re-ran SHA-256 + byte-count checks on `.vendor-repro/kokoro.web.js`. Result exactly matched the staged candidate values above.

Artifact also contains the candidate/reproduction/diff reports generated by CI.

If this artifact has expired, deterministic rebuild from the locked source remains the fallback; do not substitute different bytes.

## 7. Current Git-object transport blocker

The exact 2.1 MB file exists locally to MASTER, but the connected GitHub write actions expose `create_blob`/contents APIs only with string/base64 content fields; they do not expose a local-file parameter. The local artifact therefore cannot be passed to the Git blob endpoint directly without serializing the entire 2.1 MB file through a tool payload.

The exact candidate Git blob does not already exist in the repo, so the tree cannot simply point to an existing object.

Do **not** bypass this by:

- Actions self-commit / bot branch write;
- changing Pages architecture purely to consume CI artifacts;
- remote/expiring artifact runtime fetches;
- split/chunk runtime architecture that changes the vendor contract;
- hand-editing the minified bundle.

Only MASTER may perform repository/production writes under current governance.

## 8. Candidate branch structure

Before runtime promotion the branch is intentionally build/evidence/test-only. It added:

- deterministic m025-5 source-candidate reconstruction in `tools/rebuild-neural-vendor.sh`;
- exact candidate provenance + SHA/size staging lock in `NEURAL-VOICE-SOURCE-LOCK.json`;
- read-only candidate workflow `.github/workflows/m025-5-candidate-build.yml`;
- promotion-aware candidate behavior;
- atomic promotion regression `neural-voice-m0255-promotion-test.js`;
- Quality wiring for that regression.

The branch deliberately leaves bootstrap, Diagnostics, SW, adapter, and committed vendor runtime at m025-4 until exact bundle insertion is possible.

## 9. Atomic promotion contract

The m025-5 promotion regression enforces exactly two coherent states.

### Blocked state

- `candidate.provenance.runtimePromoted === false`
- runtime source lock remains m025-4 bundle SHA/size;
- runtime vendor file remains m025-4 bytes;
- bootstrap remains `?nv=m025-4` and m025-4 byte count;
- Diagnostics remains `m025-4`;
- SW URL/SW_REV remains m025-4.

### Promoted state

All of the following must move together in one coherent candidate head:

- exact committed vendor bytes = SHA-256 `f4329122...145ad`, size `2,136,684`;
- source-lock runtime bundle = same SHA/size;
- `candidate.provenance.runtimePromoted === true`;
- candidate state no longer says `PROMOTION_BLOCKED`;
- bootstrap vendor URL = `?nv=m025-5`;
- bootstrap asset byte count = `2,136,684`;
- Diagnostics build = `m025-5`;
- SW precache vendor URL = `?nv=m025-5`;
- `SW_REV` starts with `m025-5-`;
- focused m025-5 test expectations advance coherently;
- timeout/single-flight/privacy/fallback contracts remain unchanged.

No partial m025-5 state may pass Quality.

## 10. Machine gates

PR #37 was opened as a DRAFT control/evidence surface so exact-head PR workflows can continuously evaluate the candidate.

At PR creation head `e6771da3f96e412e5d55a494781bf08c9dacfbc6`, PR-triggered workflows included:

- MASTER Authority Guard — run `31912427622`;
- A6/A7 Automated Verifiers — run `31912427692`;
- Neural Vendor Repro — run `31912427605`;
- FIEZEL Quality — run `31912427608`.

A6 focused neural verification completed its substantive steps successfully on that head. Continue checking final run conclusions, A7, Quality, and Repro rather than assuming their final state from this document.

The normal Neural Vendor Repro is expected to fail while `runtimePromoted=false`, because it compares the source-derived m025-5 build against the intentionally still-committed m025-4 runtime bundle. That failure is an intentional promotion barrier until exact bytes are committed; the dedicated candidate workflow is the source-candidate proof during the blocked state.

## 11. Required promotion order

Once a MASTER-authorized Git write path can insert the exact bundle:

1. Verify current `main` SHA and candidate freshness first.
2. Insert only the exact proven `kokoro.web.js` bytes.
3. Promote source-lock runtime SHA/size and set `runtimePromoted=true`.
4. Advance bootstrap vendor URL + byte count to m025-5.
5. Advance Diagnostics exactly m025-4 -> m025-5.
6. Advance SW vendor URL + `SW_REV` m025-5 coherently.
7. Advance focused tests to m025-5 without weakening semantics.
8. Run exact-head candidate build, Neural Vendor Repro, Quality, MASTER Authority Guard, A6, A7.
9. Confirm PR freshness/mergeability against current `main`.
10. Only then mark PR ready and merge using expected exact head SHA.
11. Verify post-merge Quality + Pages deployment from `main:/`.
12. Fully close/reopen physical Apple standalone PWA, confirm Diagnostics m025-5, run **exactly one** neural-only `Tes Suara`, export Diagnostics immediately.
13. Classify the last eSpeak/adapter stage. Do not claim physical success unless the device actually produces the required neural audio/trace.

## 12. Diagnostic interpretation after deployment

The latest lifecycle evidence should be interpreted by the last completed stage. Existing m025-4 phase probes include eSpeak runtime/worker/init-cache/voice/synthesis boundaries. A fresh m025-5 trace must supersede old hypotheses if it reaches further.

Do not increase timeout to hide a pending boundary. A timeout is diagnostic evidence, not success.

## 13. Knowledge persistence rules

Every new meaningful finding should be stored in at least one durable GitHub surface, preferably both when appropriate:

- Issue #30 for neural technical evidence/diagnostic decisions;
- PR #37 for candidate/gate/promotion evidence;
- Issue #12 for formal MASTER control decisions;
- Issue #22 for read-only squad routing/instructions;
- this file for compact resume-from-here state.

Update this file when authoritative SHA/hash/state changes. Never leave the only copy of a critical finding in chat.

## 14. Completion rule

The project is **not finished** merely because CI is green. Do not declare `all gate pass` until required exact-head machine gates, merge/deploy gates, and the relevant physical Apple standalone neural gate have passed under the current MASTER decision.